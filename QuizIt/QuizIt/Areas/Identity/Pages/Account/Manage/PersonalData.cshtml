@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Personal Data";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h4>@ViewData["Title"]</h4>

<div class="row">
    <div class="col-md-6">
        <p>Ditt konto innehåller personlig data som du har angivit. På denna sida kan du ladda ner eller radera datan du angivit.</p>
        <p>
            <strong>Genom att radera denna data kommer ditt konto att raderas för alltid! Vill du verkligen aldrig quizza igen?</strong>
        </p>
        <form asp-page="DownloadPersonalData" method="post" class="form-group">
            <button class="btn btn-default" type="submit">Ladda ner</button>
        </form>
        <p>
            <a asp-page="DeletePersonalData" class="btn btn-default">Radera</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}